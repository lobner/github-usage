<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleName</key>
	<string>GitHub Usage</string>
	<key>CFBundleDisplayName</key>
	<string>GitHub Usage</string>
	<!-- Deliberately still githubstatus, though the app is now called GitHub
	     Usage. The bundle id is the app's identity to macOS: keeping it means the
	     existing "Open at Login" registration and the state under
	     ~/Library/Application Support survive the rename. Changing it would
	     orphan the login item and re-prompt every user. -->
	<key>CFBundleIdentifier</key>
	<string>dk.biq.githubstatus</string>
	<key>CFBundleVersion</key>
	<string>1.0</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleExecutable</key>
	<string>githubusage</string>
	<key>LSMinimumSystemVersion</key>
	<string>11.0</string>
	<!-- Agent app: lives only in the menu bar, no Dock icon, no app switcher. -->
	<key>LSUIElement</key>
	<true/>
	<key>NSHighResolutionCapable</key>
	<true/>
</dict>
</plist>
